const canvasNestConfig = {
    color: '0,255,0',
    count: 88,
    opacity: 0.7,
    zIndex: -1,
    zIndex_mode: 'behind'
};

new CanvasNest(document.getElementById('canvas-nest-container'), canvasNestConfig);
